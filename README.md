# Namaste React

//props is an object
//our website is driven by config or data that's why its confi-driven UI
//index as a key is a bad practice
// babel is a js compiler , jsx to react code
// whenever state variable changes , react re-render the pages.
// react is good at DOM operations as it has virtual DOM
// promise(is an object) is a place holder which will be filled until we recieve a future asyronous value
OR it is an object representing the eventual compeletion or failure of an asynchronous operation.
// async fn always returns a funtion  
// await can only be used inside async fn
// fetch() => response.json() => jsonValue
// async awitis syntectical sugar [promise.then/.catch]
// shimmer UI
// onChange handler
//react fibre is making react more efficient and faster
//07
-use effect includes 2 args \*a callback fn and dependency array
// npm i react-router-dom

# 2 types of routing in web apps

- client side routing (react router dom)
- server side routing (express js)
